# Copy this to tests/.env and configure for your environment

# Networking
NODE_PORT=5000
PUBLIC_DOMAIN=sukusuku.ai

# Auth test user (create a test account first)
TEST_EMAIL=test@example.com
TEST_PASSWORD=YourTestPassword123!

# Endpoints to probe (edit to match your sukusuku.ai app)
HEALTH_PATH=/
LOGIN_PATH=/login
LOGOUT_PATH=/logout

# Optional API endpoints (if present)
API_HEALTH=/api/auth/user
AI_TEST_ENDPOINT=/api/ai/generate

# Database (update with your actual database URL)
DATABASE_URL=postgres://user:password@host:5432/database
RUN_DB_WRITE_TEST=false